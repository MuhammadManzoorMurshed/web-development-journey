const computer = {
    brand: 'Lenovo', 
    price: 35000,
    processor: 'Intel',
    hdd: '512gb',
    monitor: 'HP'
}

const keys = Object.keys(computer);
const values = Object.values(computer);

console.log(keys);
console.log(values);