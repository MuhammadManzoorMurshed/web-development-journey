function tenTimes (number) {
    const result = number * 10;

    return result;

    console.log(result);
}

function cutHalf(number) {
    const half = number / 2;

    return half;
}

const tTimes = tenTimes(5);
console.log(tTimes);

const bigNumber = tenTimes(6);
console.log(bigNumber);